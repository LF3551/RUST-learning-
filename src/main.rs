fn main() {
    let b: u8;
    println!("Hello, world!");
    // Boolean
    let flag = true;
    // Char
    let ch = 'a';
    //Compound
    //Tuple
    let my_typle: (i8, char, bool) = (42, 'a', false);
    //Array
    let arr = [1, 2, 3]; //fixed
}
